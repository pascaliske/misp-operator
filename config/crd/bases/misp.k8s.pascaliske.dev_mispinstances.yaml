---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: mispinstances.misp.k8s.pascaliske.dev
spec:
  group: misp.k8s.pascaliske.dev
  names:
    kind: MispInstance
    listKind: MispInstanceList
    plural: mispinstances
    shortNames:
    - misp
    singular: mispinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.image
      name: Image
      type: string
    - jsonPath: .spec.baseUrl
      name: Base URL
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MispInstance is the Schema for the mispinstances API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Specification of the desired state of the MispInstance
            properties:
              admin:
                description: Optional admin account configuration
                properties:
                  credentialsSecretRef:
                    description: Provide a secret in the same namespace with custom
                      admin credentials
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  organisation:
                    description: Optionally customize the initial admin organisation
                    properties:
                      name:
                        description: Customize the initial organisations name
                        type: string
                      uuid:
                        description: Pre-fill the initial organisations UUID
                        type: string
                    type: object
                type: object
              baseUrl:
                description: Provide the front-facing url of the instance
                minLength: 1
                type: string
              cache:
                description: Provide your k/v based cache connection details - e.g.
                  valkey or redis
                properties:
                  enableEmptyPassword:
                    default: false
                    description: Explicitly disable the use of a password - this is
                      not recommended for production setups
                    type: boolean
                  host:
                    description: Host address of the cache
                    type: string
                  passwordSecretRef:
                    description: Reference a Kubernetes secret in the same namespace
                      containing the password
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  port:
                    default: 6379
                    description: Port the cache is listening on - defaults to "6379"
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                required:
                - host
                type: object
                x-kubernetes-validations:
                - message: at least one of the fields in [passwordSecretRef enableEmptyPassword]
                    must be set
                  rule: '[has(self.passwordSecretRef),has(self.enableEmptyPassword)].filter(x,x==true).size()
                    >= 1'
              database:
                description: Provide your database connection details
                properties:
                  credentialsSecretRef:
                    description: Reference a Kubernetes secret in the same namespace
                      containing the database credentials
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  host:
                    description: Host address of the database
                    type: string
                  name:
                    default: misp
                    description: Name of the database for the instance - defaults
                      to "misp"
                    type: string
                  port:
                    default: 3306
                    description: Port the database is listening on - defaults to "3306"
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                required:
                - credentialsSecretRef
                - host
                type: object
              email:
                description: Configure optional email notification settings
                properties:
                  smtp:
                    description: Configure SMTP settings for email notifications
                    properties:
                      host:
                        description: Provide SMTP host address
                        type: string
                      port:
                        default: 25
                        description: Provide SMTP port number
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                    type: object
                type: object
                x-kubernetes-validations:
                - message: at least one of the fields in [smtp] must be set
                  rule: '[has(self.smtp)].filter(x,x==true).size() >= 1'
              extraEnvs:
                default: []
                description: Provide extra environment variables for the core container
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
              image:
                description: Override the core image of the instance - defaults to
                  ghcr.io/misp/misp-docker/misp-core
                type: string
              imagePullPolicy:
                default: IfNotPresent
                description: Override the image pull policy for the core image - defaults
                  to "IfNotPresent"
                type: string
              imagePullSecrets:
                description: Provide image pull secrets for the core image
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              modules:
                description: Optional modules component
                properties:
                  enabled:
                    default: true
                    description: Enable optional modules component
                    type: boolean
                  image:
                    description: Override the modules image - defaults to ghcr.io/misp/misp-docker/misp-modules
                    minLength: 1
                    type: string
                required:
                - image
                type: object
              nginx:
                description: Optional nginx configuration
                properties:
                  clientMaxBodySize:
                    default: 50M
                    description: Customize the max client body size of nginx
                    type: string
                  fastcgi:
                    description: Customize FastCGI settings
                    properties:
                      connectTimeout:
                        default: 300s
                        description: Customize the FastCGI connect timeout - defaults
                          to "300s"
                        type: string
                      readTimeout:
                        default: 300s
                        description: Customize the FastCGI read timeout - defaults
                          to "300s"
                        type: string
                      sendTimeout:
                        default: 300s
                        description: Customize the FastCGI send timeout - defaults
                          to "300s"
                        type: string
                    type: object
                  forwardedHeaders:
                    description: Configure forwarded headers
                    properties:
                      enabled:
                        default: false
                        description: Enable the usage of forwarded for headers
                        type: boolean
                      trustedProxies:
                        default: []
                        description: Configure the list of trusted proxies
                        items:
                          type: string
                        type: array
                    type: object
                  image:
                    description: Override the nginx image - defaults to ghcr.io/misp/misp-docker/misp-nginx
                    minLength: 1
                    type: string
                  securityHeaders:
                    description: Configure security headers
                    properties:
                      contentSecurityPolicy:
                        description: Customize the value of the Content-Security-Policy
                          header
                        type: string
                      frameOptions:
                        default: SAMEORIGIN
                        description: Customize the value of the X-Frame-Options header
                        enum:
                        - DENY
                        - SAMEORIGIN
                        type: string
                      hstsMaxAge:
                        description: Customize the value of the Strict-Transport-Security
                          header
                        format: int32
                        minimum: 0
                        type: integer
                    type: object
                required:
                - image
                type: object
              oidc:
                description: Configure optional OIDC auth settings
                properties:
                  credentialsSecretRef:
                    description: Provide OIDC client credentials
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  enabled:
                    default: false
                    description: Enable OIDC auth mode
                    type: boolean
                  settings:
                    description: Override default OIDC settings
                    properties:
                      allowEmailLinking:
                        default: false
                        description: Allow linking of existing users by email to OIDC
                          - defaults to "false"
                        type: boolean
                      defaultOrg:
                        description: Specify the default organisation for authenticated
                          users
                        type: string
                      disablePushedAuthorizationRequest:
                        default: false
                        description: Disable the use of pushed authorization request
                          - defaults to "false"
                        type: boolean
                      disableRequestObject:
                        default: false
                        description: Disable the use of the request object approach
                          in authorization requests - defaults to "false"
                        type: boolean
                      method:
                        default: ClientSecretBasic
                        description: Specify the client authentication method to use
                          - defaults to "ClientSecretBasic"
                        enum:
                        - ClientSecretBasic
                        - ClientSecretPost
                        - ClientSecretJwt
                        - PrivateKeyJwt
                        type: string
                      mixedAuth:
                        default: true
                        description: Allow both OIDC authentication and password authentication
                          - defaults to "true"
                        type: boolean
                      offlineAccess:
                        default: true
                        description: Enable request of offline access tokens - defaults
                          to "true"
                        type: boolean
                      requireEmailVerified:
                        default: true
                        description: Require `email_verified` token claim to be true
                          - defaults to "true"
                        type: boolean
                      rolesMapping:
                        additionalProperties:
                          type: integer
                        default: {}
                        description: Specify a JSON mapping from OIDC roles to MISP
                          role IDs
                        type: object
                      rolesProperty:
                        default: roles
                        description: Specify the token claim which contains the user
                          roles - defaults to "roles"
                        type: string
                      scopes:
                        default:
                        - profile
                        - email
                        description: Request additional scopes from the Id
                        items:
                          type: string
                        type: array
                    type: object
                required:
                - credentialsSecretRef
                type: object
              storage:
                description: Optional storage configuration - defaults to a 10Gi PVC
                  with default storage class name
                properties:
                  pvcTemplate:
                    description: Optionally provide your own PVC template - see here
                      https://kubernetes.io/docs/reference/kubernetes-api/core/persistent-volume-claim-v1/#PersistentVolumeClaimSpec
                    properties:
                      accessModes:
                        description: |-
                          accessModes contains the desired access modes the volume should have.
                          More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: atomic
                      dataSource:
                        description: |-
                          dataSource field can be used to specify either:
                          * An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot)
                          * An existing PVC (PersistentVolumeClaim)
                          If the provisioner or an external controller can support the specified data source,
                          it will create a new volume based on the contents of the specified data source.
                          When the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef,
                          and dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified.
                          If the namespace is specified, then dataSourceRef will not be copied to dataSource.
                        properties:
                          apiGroup:
                            description: |-
                              APIGroup is the group for the resource being referenced.
                              If APIGroup is not specified, the specified Kind must be in the core API group.
                              For any other third-party types, APIGroup is required.
                            type: string
                          kind:
                            description: Kind is the type of resource being referenced
                            type: string
                          name:
                            description: Name is the name of resource being referenced
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                        x-kubernetes-map-type: atomic
                      dataSourceRef:
                        description: |-
                          dataSourceRef specifies the object from which to populate the volume with data, if a non-empty
                          volume is desired. This may be any object from a non-empty API group (non
                          core object) or a PersistentVolumeClaim object.
                          When this field is specified, volume binding will only succeed if the type of
                          the specified object matches some installed volume populator or dynamic
                          provisioner.
                          This field will replace the functionality of the dataSource field and as such
                          if both fields are non-empty, they must have the same value. For backwards
                          compatibility, when namespace isn't specified in dataSourceRef,
                          both fields (dataSource and dataSourceRef) will be set to the same
                          value automatically if one of them is empty and the other is non-empty.
                          When namespace is specified in dataSourceRef,
                          dataSource isn't set to the same value and must be empty.
                          There are three important differences between dataSource and dataSourceRef:
                          * While dataSource only allows two specific types of objects, dataSourceRef
                            allows any non-core object, as well as PersistentVolumeClaim objects.
                          * While dataSource ignores disallowed values (dropping them), dataSourceRef
                            preserves all values, and generates an error if a disallowed value is
                            specified.
                          * While dataSource only allows local objects, dataSourceRef allows objects
                            in any namespaces.
                          (Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled.
                          (Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled.
                        properties:
                          apiGroup:
                            description: |-
                              APIGroup is the group for the resource being referenced.
                              If APIGroup is not specified, the specified Kind must be in the core API group.
                              For any other third-party types, APIGroup is required.
                            type: string
                          kind:
                            description: Kind is the type of resource being referenced
                            type: string
                          name:
                            description: Name is the name of resource being referenced
                            type: string
                          namespace:
                            description: |-
                              Namespace is the namespace of resource being referenced
                              Note that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details.
                              (Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled.
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                      resources:
                        description: |-
                          resources represents the minimum resources the volume should have.
                          Users are allowed to specify resource requirements
                          that are lower than previous value but must still be higher than capacity recorded in the
                          status field of the claim.
                          More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources
                        properties:
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      selector:
                        description: selector is a label query over volumes to consider
                          for binding.
                        properties:
                          matchExpressions:
                            description: matchExpressions is a list of label selector
                              requirements. The requirements are ANDed.
                            items:
                              description: |-
                                A label selector requirement is a selector that contains values, a key, and an operator that
                                relates the key and values.
                              properties:
                                key:
                                  description: key is the label key that the selector
                                    applies to.
                                  type: string
                                operator:
                                  description: |-
                                    operator represents a key's relationship to a set of values.
                                    Valid operators are In, NotIn, Exists and DoesNotExist.
                                  type: string
                                values:
                                  description: |-
                                    values is an array of string values. If the operator is In or NotIn,
                                    the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                    the values array must be empty. This array is replaced during a strategic
                                    merge patch.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: |-
                              matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                              map is equivalent to an element of matchExpressions, whose key field is "key", the
                              operator is "In", and the values array contains only "value". The requirements are ANDed.
                            type: object
                        type: object
                        x-kubernetes-map-type: atomic
                      storageClassName:
                        description: |-
                          storageClassName is the name of the StorageClass required by the claim.
                          More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1
                        type: string
                      volumeAttributesClassName:
                        description: |-
                          volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim.
                          If specified, the CSI driver will create or update the volume with the attributes defined
                          in the corresponding VolumeAttributesClass. This has a different purpose than storageClassName,
                          it can be changed after the claim is created. An empty string or nil value indicates that no
                          VolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state,
                          this field can be reset to its previous value (including nil) to cancel the modification.
                          If the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be
                          set to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource
                          exists.
                          More info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/
                        type: string
                      volumeMode:
                        description: |-
                          volumeMode defines what type of volume is required by the claim.
                          Value of Filesystem is implied when not included in claim spec.
                        type: string
                      volumeName:
                        description: volumeName is the binding reference to the PersistentVolume
                          backing this claim.
                        type: string
                    type: object
                  size:
                    description: Customize the storage size of the PVC
                    type: string
                  storageClass:
                    description: Explicitly configure a storage class name for the
                      PVC
                    type: string
                type: object
              suspend:
                default: false
                description: Suspend reconciliation of this resource through the operator
                type: boolean
              timeZone:
                default: Etc/UTC
                description: Override the time zone of the containers
                minLength: 1
                type: string
              uuid:
                description: Pre-fill the instance UUID - defaults to an auto generated
                  one
                minLength: 1
                type: string
            required:
            - baseUrl
            - cache
            - database
            type: object
          status:
            description: Most recently observed status of the MispInstance. This data
              may not be up to date. Populated by the system. Read-only.
            properties:
              conditions:
                description: The status of each condition is one of True, False, or
                  Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              image:
                type: string
              lastReconcileTime:
                description: LastReconcileTime is the timestamp of the last reconciliation
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  state
                type: string
              phase:
                enum:
                - Pending
                - Running
                - Failed
                - Suspended
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
